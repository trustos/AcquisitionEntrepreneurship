# OS / editor cruft
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc

# temp files, locks, and render previews
*.tmp
.~lock.*#
~$*
page-*.jpg
verify-*.jpg

# Derived aggregates — regenerate with rebuild_log.py / rebuild_dataset.py.
# Ignore the binary/large ones so teammates never hit a merge conflict; the
# per-deal deals/*/record.json files are the source of truth (never collide).
deals/deal-log.xlsx
deals/deals.json
# (deals/deal-log.csv stays tracked as a small human-diffable index — still derived.)

# session-only working docs — not part of the repo
handoff/
